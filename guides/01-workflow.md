# Workflow: Task to Merge

1. Define task in one sentence with explicit acceptance criteria.
2. Ask agent to inspect current code and constraints first.
3. Require minimal diff and tests.
4. Run checks (`lint`, `test`, `build`).
5. Request a risk-focused review before merge.
